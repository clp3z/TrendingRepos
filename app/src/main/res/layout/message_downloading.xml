<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="15dp"
        android:orientation="vertical"
        android:background="@android:color/white">

        <ImageView
            android:id="@+id/test"
            android:layout_width="@dimen/message_image_size"
            android:layout_height="@dimen/message_image_size"
            android:src="@drawable/ic_download"
            android:contentDescription="@string/image_downloading"/>

        <TextView
            android:text="@string/label_downloading"
            style="@style/TitleTextViewStyle" />

        <TextView
            android:text="@string/label_downloading_from_server"
            style="@style/SubtitleTextViewStyle"/>

        <ProgressBar
            android:id="@+id/progress_circular"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"/>

    </androidx.appcompat.widget.LinearLayoutCompat>

</layout>